# AIMEGApro 项目说明书

> 项目名称：ai-outpainting-nextjs  
> 版本：0.0.1（私有）  
> 标语：Democratizing AI: Easy Access for Everyone  
> 项目类型：AI 图像外绘（Outpainting）Web 应用  
> 代码总量：约 13,000+ 行（不含 node_modules/.next）

---

## 一、技术栈

### 1.1 核心框架

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 14.2.4 (App Router) | 全栈框架，服务端渲染 + API 路由 |
| React | 18.3.1 | UI 组件库 |
| TypeScript | 5.5 | 类型安全 |
| Yarn | 1.22 | 包管理器 |

### 1.2 UI / 样式

| 技术 | 版本 | 用途 |
|------|------|------|
| Tailwind CSS | 3.4.4 | 原子化样式 |
| NextUI | 2.0 | UI 组件库 |
| Ant Design | 5.18.3 | 企业级 UI 组件（表格、表单等） |
| Framer Motion | 11.2 | 动画库 |
| react-konva / konva | 8.4 | Canvas 图像编辑器 |
| next-themes | 0.3 | 明暗主题切换 |
| @tabler/icons-react + lucide-react | - | 图标库 |
| sugar-high | 0.7 | 代码语法高亮 |

### 1.3 认证

| 技术 | 版本 | 用途 |
|------|------|------|
| next-auth | 5.0.0-beta.19 | Google OAuth 登录 |
| @auth/prisma-adapter | 2.4 | Prisma 适配器 |
| iron-session | 8.0 | 匿名用户 Cookie Session |
| bcryptjs | 2.4 | 密码哈希 |

### 1.4 数据库

| 技术 | 版本 | 用途 |
|------|------|------|
| Prisma | 5.16 | ORM |
| PostgreSQL (Supabase) | - | 关系型数据库 |

### 1.5 支付

| 技术 | 版本 | 用途 |
|------|------|------|
| PayPal (REST SDK + React) | - | PayPal 支付 |
| Stripe (React + 服务端 SDK) | 16.0 | Stripe 支付 |

### 1.6 AI / ML

| 技术 | 版本 | 用途 |
|------|------|------|
| Vercel AI SDK | 3.4 | AI 流式调用 |
| @ai-sdk/openai | 0.0.65 | OpenAI API |
| RunPod SDK | 1.0 | GPU 服务器管理 |
| Gradio | 1.17 (dev) | HuggingFace 模型交互 |
| 通义千问 DashScope | - | 阿里云 LLM（自定义 JS 实现） |

### 1.7 存储

| 技术 | 用途 |
|------|------|
| Cloudflare R2 (S3 API) | 主文件存储 |
| 腾讯云 COS | 备选文件存储 |
| @aws-sdk/client-s3 | S3 签名 URL 生成 |

### 1.8 国际化

| 技术 | 版本 | 用途 |
|------|------|------|
| LinguiJS | 4.13 | 国际化框架（SWC 插件） |
| 支持语言 | - | 26 种语言 |

### 1.9 内容管理

| 技术 | 用途 |
|------|------|
| MDX | 博客 + AI 工具目录内容 |
| next-mdx-remote | 服务端 MDX 渲染 |
| gray-matter | 前置元数据解析 |

### 1.10 开发工具

| 技术 | 用途 |
|------|------|
| ESLint | 代码规范 |
| Prettier | 代码格式化 |
| PostCSS + Autoprefixer | CSS 处理 |
| Wrangler | Cloudflare Workers CLI |

### 1.11 日志

| 技术 | 用途 |
|------|------|
| Pino | 日志库 |
| next-logger | Next.js 日志集成 |
| rotating-file-stream | 日志文件轮转 |

### 1.12 依赖规模

- **dependencies**: 约 60 个直接依赖
- **devDependencies**: 约 25 个开发依赖
- **TypeScript 严格模式**: 开启

---

## 二、可优化部分

### 2.1 代码架构优化

| 问题 | 说明 | 建议 |
|------|------|------|
| **代码重复：tools/ 与 blogs/ 目录** | `framework/tools/tools.ts` 与 `framework/blogs/blogs.ts` 功能高度重复，`mdx.tsx` 完全重复 | 抽取公共模块复用 |
| **自定义路由系统冗余** | `framework/utils/routes.ts` 实现了 `ARouter` 类（114 行），但 Next.js App Router 已内置完整路由能力 | 评估是否可移除 |
| **ue-forms.tsx 空占位** | `framework/components/ue-forms.tsx` 内容为空 | 清理或补全实现 |
| **hooks/ 和 utils/ 仅做 re-export** | 顶层 `hooks/index.ts` 和 `utils/index.ts` 仅重导出 `framework/` 下的内容，增加层级复杂度 | 删除冗余导出层或统一引用路径 |

### 2.2 代码质量 / Bug

| 问题 | 说明 | 建议 |
|------|------|------|
| **Prisma 单例有缺陷** | `config/prisma.ts` 中 `if (!prisma)` 检查无效，`prisma` 变量每次调用都被重置为 `undefined`，依赖模块缓存才工作 | 使用全局对象 `globalThis.prisma` 模式 |
| **MDX 中使用 eval** | `framework/tools/mdx.tsx` 中 `Table` 组件对 `headers` 和 `rows` prop 使用了 `eval()` | 用 JSON.parse 替代 eval |
| **nanoid 未在 package.json 声明** | `lib/basic/services/TradeService.ts` 中 `import { nanoid } from 'nanoid'`，但 `nanoid` 不在依赖中 | 添加到 package.json |
| **file-saver 未声明** | `ues34r2.ts` 中引用 `file-saver`，但未在依赖清单中 | 添加到 package.json |

### 2.3 性能优化

| 问题 | 说明 | 建议 |
|------|------|------|
| **静态消息文件全量导入** | `framework/locale/messagesLoader.ts` 静态 import 所有 26 种语言的翻译 JSON（每个约 60KB） | 改为动态 import 按需加载 |
| **编辑器组件过大** | `editor/view.tsx` 1012 行，单一组件管理大量状态和逻辑 | 拆分为多个子组件（画布、上传、支付、历史） |
| **缺少图片优化** | 项目中多处使用 `<img>`（eslint 配置 `no-img-element: off`） | 迁移至 Next.js `<Image>` 组件 |
| **blog 页面缺少增量生成** | `blogs/[slug]/page.tsx` 使用了 `generateStaticParams`，但 MDX 内容变更后需全量重构建 | 使用 `revalidate` / ISR |

### 2.4 安全优化

| 问题 | 说明 | 建议 |
|------|------|------|
| **eval() 在 MDX 中** | 如前所述，`eval()` 存在 XSS 风险 | 立即修复 |
| **CSRF 保护** | API 路由（如 order、trade）未见 CSRF token 验证 | 添加 CSRF 防护 |
| **环境变量暴露** | `next.config.mjs` 中约 20 个 `UE_*` 和 `AUTH_*` 变量暴露给客户端 | 确认是否所有变量都需要客户端可见 |

### 2.5 工程化 / DevOps

| 问题 | 说明 | 建议 |
|------|------|------|
| **缺少 Docker 化** | 无 Dockerfile / docker-compose | 添加容器化部署 |
| **缺少 CI/CD** | 无 GitHub Actions / GitLab CI 配置 | 配置自动化构建和部署 |
| **缺少测试** | 项目中无测试文件（jest/cypress 等） | 添加单元测试和 E2E 测试 |
| **缺少 API 文档** | 无 OpenAPI / Swagger 文档 | 添加 API 文档 |

### 2.6 用户体验

| 问题 | 说明 | 建议 |
|------|------|------|
| **缺少加载状态** | 部分页面和组件缺少 skeleton/skeleton 加载态 | 增加全局加载状态组件 |
| **错误处理** | 部分 API 路由和组件错误处理较简略 | 统一错误处理规范 |
| **响应式** | `user-case/page.tsx`（615 行静态内容）缺少响应式适配 | 适配移动端布局 |

### 2.7 依赖管理

| 问题 | 说明 | 建议 |
|------|------|------|
| **next-auth 仍为 beta** | `next-auth@5.0.0-beta.19`，API 可能变动 | 关注正式版发布后升级 |
| **部分依赖版本较旧** | `react-use` 等次要依赖版本较低 | 审查并更新过时依赖 |
| **Yarn 与 npm 混用** | `packageManager` 指定 Yarn，但 `package-lock.json` 也存在 | 统一包管理器 |

---

## 三、现有代码难易程度注释

> 难度分级：⭐ 简单 | ⭐⭐ 中等 | ⭐⭐⭐ 复杂

### 3.1 核心页面

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `app/[lang]/(editor)/editor/view.tsx` | 1012 | ⭐⭐⭐ | 核心 AI 图像编辑器，集成 Konva 画布、Gradio AI 调用、SSE 轮询、支付、上传等，是项目最复杂的组件 |
| `app/[lang]/(public)/user-case/page.tsx` | 615 | ⭐ | 静态营销页面，大量 JSX 但无状态和交互逻辑，纯展示型 |
| `app/[lang]/(public)/(tools)/tools/[slug]/BlogPostList.tsx` | 322 | ⭐⭐ | 列表/详情切换模式，含动态导入 MDX 渲染 |
| `app/[lang]/(public)/(pricing)/pricing/page.tsx` | 34 | ⭐ | 极简编排页，委托给子组件 |
| `app/[lang]/(public)/(blogs)/blogs/[slug]/page.tsx` | 55 | ⭐ | 服务端博客详情页，MDX 渲染 |
| `app/[lang]/(public)/ai-image-extender/page.tsx` | 68 | ⭐ | AI 工具落地页编排，委托子组件 |
| `app/[lang]/(public)/(faq)/faq/page.tsx` | ~30 | ⭐ | 静态 FAQ 页面 |

### 3.2 API 路由

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `app/api/order/[[...slug]]/route.ts` | 114 | ⭐⭐ | 订单 CRUD，含 Prisma 事务处理积分和回调退款 |
| `app/api/payment/[[...path]]/route.ts` | ~80 | ⭐⭐ | PayPal/Stripe 支付代理跳转 |
| `app/api/auth/[...nextauth]/route.ts` | ~5 | ⭐ | NextAuth 标准配置 |
| `app/api/cos/route.ts` | ~20 | ⭐ | COS 临时凭证 |
| `app/api/s34r2/route.ts` | ~15 | ⭐ | S3/R2 临时凭证 |
| `app/api/dict-data/route.ts` | ~20 | ⭐ | 字典数据查询 |

### 3.3 框架核心

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `framework/components/ue-grids.tsx` | 503 | ⭐⭐⭐ | 通用网格/表格组件，含分页、查询、骨架屏、多种单元格渲染 |
| `framework/utils/cos.ts` | 171 | ⭐⭐ | 腾讯云 COS 服务端凭证和签名 URL 生成 |
| `framework/utils/requests.ts` | 137 | ⭐⭐ | HTTP 请求工具 + R 响应构建器 + 自定义路由匹配 |
| `framework/utils/routes.ts` | 114 | ⭐⭐ | 自定义路由抽象类 |
| `framework/utils/nicknames.ts` | 393 | ⭐ | 静态昵称数组，无逻辑 |
| `framework/utils/JWTUtils.ts` | 48 | ⭐ | JWT 编解码工具 |
| `framework/utils/s34r2.ts` | 50 | ⭐ | S3 兼容存储客户端 |
| `framework/utils/formats.ts` | 85 | ⭐ | 日期/数字格式化工具 |
| `framework/services/TradeService.ts` | 94 | ⭐⭐ | 交易服务，含原子性数据库事务 |
| `framework/services/OrderService.ts` | 94 | ⭐⭐ | 订单服务，含 S3 签名 URL 生成 |
| `framework/services/DictDataService.ts` | 77 | ⭐ | 字典数据 CRUD |
| `framework/services/DictService.ts` | 62 | ⭐ | 字典管理 CRUD |

### 3.4 国际化

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `framework/locale/messagesLoader.ts` | ~60 | ⭐⭐ | 26 种语言翻译消息静态导入和转换 |
| `framework/locale/locale.ts` | ~80 | ⭐ | 语言环境配置和工具函数 |
| `framework/locale/serverSideTranslations.ts` | ~30 | ⭐ | SSR 翻译注入 |

### 3.5 上传组件

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `framework/components/ue-upload/` (6 files) | ~350 合计 | ⭐⭐ | 文件上传子系统，支持 COS 和 S3/R2 双云存储 |
| `framework/hooks/useCosUploader.ts` | 105 | ⭐⭐ | COS 上传 Hook，含会话缓存和动态凭证获取 |

### 3.6 LLM / AI 集成

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `framework/llm/utils.ts` | 147 | ⭐⭐ | 流式请求实现（async generator + ReadableStream） |
| `framework/apis/QwenChatApi.js` | ~120 | ⭐⭐ | 通义千问 LLM API 封装，含流式和非流式调用 |
| `framework/llm/QwenModel.ts` | ~50 | ⭐ | Qwen 模型定义 |
| `framework/apis/ChatApi.ts` | ~60 | ⭐ | LLM 聊天 API |

### 3.7 认证

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `auth.ts` | 54 | ⭐⭐ | NextAuth 配置（Google OAuth、Session、积分赠送回调） |
| `config/plain-auth.ts` | ~150 | ⭐⭐ | 匿名用户 iron-session Cookie 认证 |
| `config/auth-config.ts` | ~80 | ⭐ | Google OAuth 配置 |
| `framework/hooks/useAuthUser.tsx` | 70 | ⭐ | 认证用户 Context Provider |

### 3.8 脚本工具

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `scripts/generator-website.js` | 193 | ⭐⭐ | AI 网站内容生成器（TDK、FAQ、博客） |
| `scripts/translate-locale.js` | 130 | ⭐⭐ | 多语言翻译自动化（并发 + 重试） |
| `scripts/translate-blogs.js` | ~200 | ⭐⭐ | 博客批量翻译脚本 |
| `scripts/openai-chat.js` | ~80 | ⭐ | OpenAI API 封装 |
| `scripts/add-word-locale.js` | ~100 | ⭐ | 翻译词典维护 |

### 3.9 杂项

| 文件 | 行数 | 难度 | 说明 |
|------|------|------|------|
| `schema.prisma` | 271 | ⭐⭐ | 数据库模型定义（User、Order、Trade、Account、DictData 等） |
| `next.config.mjs` | 82 | ⭐ | Next.js 配置（SWC 插件、Webpack、重定向） |
| `middleware.ts` | 42 | ⭐⭐ | 语言检测 + NextAuth 边缘认证 |
| `framework/event/eventBus.ts` | 22 | ⭐ | 精简发布订阅事件总线 |
| `tailwind.config.ts` | ~60 | ⭐ | Tailwind 主题配置 |
| `lingui.config.js` | ~20 | ⭐ | 国际化配置 |

### 3.10 整体难度评估

```
项目整体难度: ⭐⭐ (中等偏复杂)

┌────────────────────────────────────────────┐
│  简单 (⭐)   占比 ~40%  │ 静态展示页、配置文件、工具函数  │
│  中等 (⭐⭐)  占比 ~40%  │ API 路由、服务层、国际化系统   │
│  复杂 (⭐⭐⭐) 占比 ~20%  │ 编辑器、网格组件、支付编排      │
└────────────────────────────────────────────┘
```

### 3.11 适合新手的入门路径

1. **⭐ 开始** - 阅读 `app/(public)/` 下简单静态页面的代码模式
2. **⭐ 理解** `config/` 和 `framework/utils/` 中的工具函数
3. **⭐⭐ 进阶** - 理解 `framework/hooks/` 的 Hook 模式和服务层代码
4. **⭐⭐ 深入** - 阅读 API 路由（`app/api/`）和支付流程
5. **⭐⭐⭐ 掌握** - 编辑器组件（`view.tsx`）和网格组件（`ue-grids.tsx`）

---

*文档生成日期：2026-07-09*
*项目代码基于 git commit: cc1bb10*
